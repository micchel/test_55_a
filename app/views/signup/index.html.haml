.signup-page
  = render "shared/sub-header"
  %main.signup-main
    %section.signup-container
      %h2.signup-container__text
        新規会員登録
      .signup-container__inner
        .signup-container__inner__content
          = link_to '/users/new',class:'signup-mail-links' do
            = fa_icon 'envelope', class: "signup-icon"
            メールアドレスを登録する
          = link_to 'https://ja-jp.facebook.com/',class:'signup-facebook-links' do
            = fa_icon "facebook-official", class: "signup-icon,facebook-icon"
            Facebookを登録する
          = link_to '', class: "signup-google-links" do
            Googleで登録する
  = render "shared/sub-footer"